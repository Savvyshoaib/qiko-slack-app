# --- Production (Railway): OAuth multi-workspace — do NOT set SLACK_BOT_TOKEN ---
SLACK_SIGNING_SECRET=your_signing_secret
SLACK_CLIENT_ID=your_client_id
SLACK_CLIENT_SECRET=your_client_secret
SLACK_STATE_SECRET=use-a-long-random-string-here

# Public HTTPS URL (Railway sets RAILWAY_PUBLIC_DOMAIN; or set explicitly)
# SLACK_APP_URL=https://your-app.up.railway.app

PORT=3001
QIKO_API_BASE_URL=https://stage-backend.qiko.ai/api/avatar

# --- Local dev only (single workspace) ---
# SLACK_BOT_TOKEN=xoxb-your-bot-token
# SLACK_APP_TOKEN=xapp-your-app-token
